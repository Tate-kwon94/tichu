.DS_Store
node_modules/
*.log
npm-debug.log*
.env
ml/.venv/
ml/*.pt
ml/*.json
# 학습 교환 선형 가중치 — 게이트 통과 산출물은 추적
!ml/weights-exchange-*.json
# 4단 RL 후보(승단전용) — CI 러너가 체크아웃해 쓴다
!ml/weights-rl4-*.json
# 롤아웃 정책 증류본(5단 후보) — CI 러너가 체크아웃해 쓴다
!ml/weights-fastpol.json
ml/*.jsonl
ml/data/
