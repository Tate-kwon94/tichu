# 3단 로드맵 — 무엇이 닫혔고, 무엇이 남았나

작성 2026-07-25. 배포 상태: **2단**(커밋 `b021be9`, https://tichu.kwon.work 라이브).
이 문서는 3단을 나중에 재개할 때 **같은 벽에 다시 부딪히지 않도록** 남긴다.

---

## 0. 최종 판정 (2026-07-26) — 3단 종결

**RL 연장(게이트2, gen13~24)까지 완주한 최종 결론: 3단 보류.**

| 단계 | 결과 |
|---|---|
| 킬 게이트 (gen21-24 평균 ≥ +6.0) | **+4.77 — 킬** (55% 정식 3단 닫힘) |
| 선발전 A (6후보 × 새시드 4,000딜) | swa13_18 우승 +5.07±1.00 (SWA가 개별 세대 전부 이김) |
| **재확정 B (새시드 6,000딜)** | **+2.77±0.57, CI하한 +1.65 — 완화 기준(+4.4)에도 미달** |

A→B 회귀(+5.07→+2.77)는 6개 중 최댓값 선택 편향 + 시드 분산 — 방법론 교훈 3의 재확인.
B가 무편향 추정이고, 통합 워크플로의 기전 계산(51.7%)과 정확히 일치:
**RL이 산 것 = raw +2.8점/라운드 ≈ 배포 점수차 51.6%.** 실재하지만(0에서 4.9SE) 작다.

- 60% 기준: 산술적으로 불가(필요 결정 변경 11.0개/라운드, RL이 산 것 1.87개)
- 55% 기준: 킬 게이트에서 기울기 사망
- 완화 53% 기준: 재확정 B에서 미달
- 사다리 등재 없는 잔여물: swa13_18(최강 raw 정책), 필요시 혼자연습 전용 "도전자 봇" 소재

## 0-보론. 아키텍처 변경 재개 — 종반 완전탐색 모듈 (2026-07-26)

§0의 "보류"는 **기존 아키텍처(flat PUCT + 플레이아웃 리프) 안에서의 종결**이었다.
사용자 인터뷰(전술 6종 = 전부 순차 계획 능력) 후 아키텍처 변경 허가가 나와 재개:
**종반(전원 손패 ≤4)은 결정화 세계가 완전정보 → 알파베타로 정확히 풀 수 있다.**
계획·카운팅·템포가 트리에서 공짜로 나온다 — 플레이아웃 근사가 가장 부정확한 구간을 정확해로 교체.

- 구현: `ml/endgame.js` — solve(결정화 세계, 알파베타, 노드캡 6000) + K=12세계 다수결(endgameMove)
- 끝단 게이트(보통봇 생태계, 짝지음 6,000딜): **+5.62±0.48 점/라운드, CI하한 +4.68** — 기준(+4.4) 통과
  - 원투 기여 +3.17, 발동 4.20회/라운드, 보통봇과 다른 수 8.8%
- 적대 검토(워크플로 5에이전트) 결과: 솔버 버그 없음. 단 **게이트 기준선은 보통봇 대체 효과** —
  950ms PUCT는 모듈≠보통봇 국면의 41%에서 이미 같은 수를 둠 → 실전 마진은 이보다 작다.
- 따라서 승단 판정은 **eval-hybrid 짝지음(2단+모듈 vs 2단, 양측 PUCT 950ms)** 실물전으로만 한다.
- 배선: eval-hybrid `TICHU_CAND=exchange,endgame` (env: TICHU_EG_CAP=4, TICHU_EG_K=12, TICHU_EG_NODECAP=6000)

**승단전 결과(2026-07-26): 실패 — 실전 전이 안 됨.**
2단+종반모듈 vs 동결 2단, 양측 PUCT 950ms, 짝지음 224게임(112쌍, 1,045라운드, perDecision 1119~1201 정상):
**+0.76 ± 3.69 점/라운드 (환산 50.5%, 명목 49.1%)** — 53% 기준선(+4.17)에 크게 미달, 0과 구분 불가.

원인(검토 경고보다 더 강했음): **950ms PUCT는 손패 ≤4 종반에서 이미 거의 정확하다.**
트리가 작아 결정론적 플레이아웃 평가가 사실상 완전탐색과 등가 — 종반모듈이 이기던 상대는
보통봇 휴리스틱이지 PUCT가 아니었다. §3의 "RL 프라이어 비전이"와 동일 구조의 교훈:
**보통봇 생태계 게이트 통과 ≠ PUCT 전이. 앞으로 어떤 개선 후보든 최종 판정은 반드시
eval-hybrid 실물전(PUCT 950ms 짝지음)으로만.** HANDCAP 5~6 확장도 같은 이유로 기대 낮음
(풀이 비용은 폭발: ≤6에서 p90 4.6s·28% 캡). → **3단 보류 유지. 종반모듈은 닫힌 경로 표에 추가.**
자산: ml/endgame.js(솔버), ml/eval-endgame.js(끝단 게이트), eval-hybrid endgame 배선(TICHU_CAND).

## 0-보론2. 완화 기준(53%) 전면 재심사 → 스택 전략 (2026-07-26 오후)

기준이 65%(+22)→53%(+4.17)로 내려오며 전략이 바뀐다: 큰 광맥 하나가 아니라 **검증된 양수를
쌓아 넘는 것**이 정당한 경로가 된다. §3의 음수/중립 사망 15개 축은 그대로 사망(기준 무관).
재심으로 살아난·새로 생긴 재료:

| 재료 | 근거 | 상태 |
|---|---|---|
| swa13_18 프라이어 | §0의 "환산 51.6%"는 계수 0.8 곱한 추정 — 직접 실물전 1차 **+7.61±4.18(55.5%)** | 확대 448게임 측정 중 |
| 종반 완전탐색 | 실물 +0.76±3.69 — 미확정 양수, 유지비 0 | 스택 재료 후보 |
| guardTichu | 파트너 티츄 배신 실측(3페어 4/4 완주). 원인=결정화가 "선언=강패" 정보를 모름 → 하드 가드 | 사다리 효과 미측정 |
| exchTriple | 랭크 3장+ 보존(현행은 2222 폭탄도 나눔) — 사용자 피드백, 단위검증 완료 | 사다리 효과 미측정 |
| 학습 교환 | 상한 +4.44(+3.2%p)·전이 함정 없음(역사적 계수 1.8) — "작다"는 65% 시대 판정 | 라벨러 준비 완료(트리플 후보 포함) |

**스택 V1 승단전 결과(960게임, 시드 8001~, 사전등록): 미달 — +0.45±2.26 (50.3%), 명목 48.8%.**
퍼즐: swa 단독 +4.37(명목 56.2%, 448게임)이었는데 검증 재료 3종을 더하니 명목이 유의하게(z≈2.6)
떨어졌다. 용의자: ①swa 단독 수치의 낙관 편향(순차 시험의 승자의 저주) ②단독 미측정
재료(특히 guardTichu — 가망 없는 봇 티츄까지 보호해 승기를 놓칠 수 있음)의 역효과.
**분해 완결(2026-07-27)**: 같은 시드 순수 swa 960게임 = +1.45±1.94 → **swa 1,408게임 풀링
+2.30±1.60(51.7%) — 원래 기전 추정(raw+2.77×0.8=51.6%)과 일치. +4.37은 낙관 표본.**
추가 3종 짝지음 순효과 −1.00±2.94(무효). swa 스택 경로 종결.

**학습 교환 단계1 통과(2026-07-27)**: 오라클 라벨 2,000개(600세계, 2단 생태계) →
특징 17차원 선형 랭커(JS 단일 소스 특징 + numpy) → **신선 20,000딜 짝지음 +2.18±0.75(2.9σ)**.
킬 기준(+1) 통과. 산수: swa(+2.3) + 학습교환(+2.2, 전이≥1 기대) ≈ +4.5 — 53% 사정권.
다음: 라벨 10k(생성 중) → 단계2 MLP → **swa+learnExch 조합 승단전**(eval-hybrid 배선 완료).

**사용자 결정(2026-07-26): 피드백 재료(guardTichu·exchTriple)는 2단에 반영하지 않음 — 3단 전용.**
2단은 검증된 상태 그대로 완전 동결(사람 파트너 배신 현상 포함). 배신 해소는 3단의 가치가 된다.
스택 규율: 부품별 게이트 → **스택 전체를 새 시드 실물전(사전등록 53%, CI 하한 기준)으로만 승격.**
여러 후보를 같은 문턱에 반복 시험하므로 최종 확정 전 부품 조합의 상쇄(①+② 전례) 반드시 재확인.

## 0-보론3. ★3단 성립·배포 + 4단 1차 시도 종결 (2026-07-27)

**3단 확정**: swa13_18(RL 24세대 SWA) + 손패별 학습 교환(선형 랭커 17특징).
CI 1,920게임 사전등록 승단전 **+6.16±1.46 (54.4%), CI[+3.30,+9.02]** — 통과.
배포 완료(sw v29, Elo 앵커 1100). 되살아난 두 축의 공통점: **"추정으로 죽였던 것"과
"이전 기준에선 작다고 접었던 것"** — 기준이 바뀌면 닫힌 경로표도 재심사해야 한다.

### 4단 1차 시도 — 전 재료 미달 (동결 3단 상대)

| 재료 | 결과 | 판정 |
|---|---|---|
| 봉황 가드 | −3.64±2.08 (960게임) | 탈락 |
| 늦은 티츄 선언 | +0.25±1.94 | 탈락(중립) |
| 카운팅 소원 | +1.14±2.14 | 보류(발동 희소로 해상 불가) |
| 교환 MLP(600세계 라벨) | 실물 −0.48±0.48 (40k딜) | 탈락 — 홀드아웃 +3.56과 정반대 |
| 교환 MLP(2,000세계 라벨·3단 생태계) | 실물 **+0.59±0.36** (60k딜, 1.6σ) | 보류 — 유의 판정에 ~20만 딜 필요, 비용 대비 무의미 |
| **RL2 SWA(gen9~16)** | PUCT **+0.10±2.08** (960게임) | **미달** |
| **RL2 gen12(최고 세대)** | PUCT **−1.65±2.02** | **미달** |

**RL2 교훈 두 가지**: ①raw 트립와이어가 세대마다 +0.87~+4.02로 요동쳤지만 PUCT 실물은
전부 0 근처 — **강해진 상대(3단)에서는 raw 개선의 전이 계수가 사실상 소멸**했다.
②개별 최고 세대(gen12 raw +4.02)가 실물에서 SWA보다 나빴다 — 세대 고점은 시드 요행.

**결론: 이 방법론(규칙층 재료 + best-response RL)의 수확은 3단에서 고갈됐다.**
4단은 구조 변경이 전제 — 로드맵 §4-3, 또는 아래 새 축.

## 0-보론4. 4단 2차 시도 종결 — 발굴 스윕·심층사고, 그리고 프록시 천장의 완성 (2026-07-27~28)

CI를 최대 활용한 2차 시도(CRN 도입 → 발굴 스윕 → 심층사고 모듈)의 전 과정과 종결.

1. **CRN(공통난수)**: `__TICHU_RNG` 주입(커밋 6a1ded/7f274eb 계열). 비트 재현은 불가
   (시간예산 → 시뮬 수 가변) — 가치는 분산 축소로 한정. 정직 재규정.
2. **발굴 스윕(2,000게임) + 홀드아웃 재스윕(1,000게임)**: 배포 PUCT vs 심층 오라클
   (후보 전수 × 250세계 CRN) 불일치 30.8%. 승자의 저주(~+5점) 제거 후에도
   **티츄 국면 격차 실재**: 리드에서 T상 23.8 / T파 20.5 / T내 18.4점.
3. **심층사고(deepThink) 모듈**: 스윕 상위 국면(티츄 활성 + 초중반 리드)에서 PUCT 대신
   후보 전수 × 200세계 CRN 직접 평가. **실물 게이트(CI 960게임, 950ms, vs 동결 3단):
   −1.16±1.88 (49.2%), CI[−4.84,+2.52] — 미달.**

**프록시≠실물 3차 확인이자 기전의 완성**: 홀드아웃 격차의 정의는 "보통봇 세계 플레이아웃
기준으로 오라클 수가 더 좋다"였다. 즉 **대리 평가자를 더 잘 최적화한 것**이고, 실전에서
PUCT의 "부정확해 보이는" 수들은 프라이어(swa)의 실전 지식이 대리 평가자의 맹점을 보정한
결과였다. c 스윕에서 양극단(c=0.5, SH)이 다 나빴던 것, RL 전이 소멸, MLP 홀드아웃-실물
역전이 전부 같은 그림: **배포봇은 평가자+프라이어 계의 고정점에 있고, "대리 평가자 기준
개선"은 어느 방향이든 균형을 깨서 손해다.** (이번엔 repCap 같은 계측 오류 없이 확정.)

**4단의 남은 세 갈래**(사용자 결정: **3안 기본**):
① 52% 완화 — 보류 조각(MLP +0.59, 소원 +1.14) CRN 스택 재게이트 (성공 ~40%, 사다리 희석)
② 리그 자가대전 — 전이 벽 돌파 가설에 2~3일 (성공 ~10%)
③ **사람 기보 축 — 기보 수집(라이브) 후 수 주 뒤 실전 데이터로 재개** ← 채택

### 3안 기반 작업 (2026-07-28, 대기 기간의 가치 작업)

- **기보 파이프라인 완성**: `ml/gamelog-replay.js`(hands0+acts→Game 복원, 라운드트립
  검증 — 로컬 11/11 통과, 최종 점수까지 일치), `ml/gamelog-vs-bot.js`(사람 결정마다
  배포 3단 PUCT 950ms 재현 비교 + 홀드아웃 CRN 격차 → mine-analyze.py 호환 +
  플레이어별 요약). **기보가 쌓이면 즉시 "사람이 3단을 이기는 국면" 지도 수확 가능.**
  주의: 평가자는 여전히 보통봇 플레이아웃 프록시 — 지도는 후보 발굴까지, 판정은 실물 게이트.
- **다음 패치 번들(미배포)**: 악마 봇 제거(366b9f8) + `/gamelog/status`(수집 확인)
  + `/gamelog/recent`(이번 부팅 기보) + `/gamelog/get?key=`(과거 부팅 KV 수거,
  `tichu:gamelog:` 접두사 제한) + `KV.list(prefix)`. 테스트 `test/e2e-gamelog-http.js`
  (소켓 없는 in-process, fetch 스텁).

## 1. 결론 요약

**2단은 이 아키텍처·이 하드웨어에서 배포 가능한 실질 상한이다** (신뢰 **90~95%**).
2026-07-25 마지막 미개척 축(상대-연속 편향)까지 실전 킬게이트로 닫혔다(§4-1).

배포봇 = PUCT(깊이1 flat bandit) + 균일 결정화 + 보통봇 플레이아웃 리프 + v6 정책 프라이어.
이 구조의 천장은 **"결정화 보통봇 플레이아웃 평가의 정확도"**로 정해지며,
그 평가는 자기 연속에 대해 **결정론적·정확**하고 PUCT가 이미 그 최선수를 실현한다.

넘어서려면 *"빠르면서 결정화 플레이아웃보다 정확한 리프 평가"*가 필요한데,
이 하드웨어·데이터에서는 존재하지 않는다(아래 §3).

---

## 2. 예산 — 실제 배포는 온라인·솔로 모두 ~950ms

| 모드 | 예산 | 코드 |
|---|---|---|
| 온라인 | **950ms** | `server/rooms.js:226` |
| 혼자 연습 | **900ms** | `client/offline.js:77` |

| 예산 | clairvoyance(완전정보 PUCT) vs 2단 | 해석 |
|---|---|---|
| **950ms = 실제 배포** | **47.5%** | **정보 포화 — 헤드룸 없음** |
| 300ms (측정용 축소모형) | ~55~67% | 세계 부족 → 분산 지배. **배포에 없는 예산** |

**함정(내가 빠졌던 것)**: 측정을 빨리 하려고 300ms 축소모형을 쓰다가 "온라인이 300ms"라고 착각했다.
그 위에서 "300ms 정보 헤드룸 발견!"이라며 여러 실험을 돌렸는데, **배포에 존재하지 않는 예산**이었다.
→ **축소모형으로 재되, 결론은 반드시 배포 예산에서 재확인하라.** 300ms 결과는 배포 판단에 쓰지 마라.

### 그런데 그 헤드룸은 잡을 수 없다 (핵심)

부분 clairvoyance 스윕 (300ms, pin = 상대 카드를 진짜 위치에 고정하는 비율):

| pin | 0.00 | 0.25 | 0.50 | 0.75 | 1.00 |
|---|---|---|---|---|---|
| 점수차 | −4.73 | +0.93 | +0.50 | −4.39 | **+6.92** |
| 승률환산 | 46.6% | 50.7% | 50.4% | 46.8% | **55.0%** |

(pin=1.0은 별도 clairvoyance 측정 두 번과 일관: 55.6%, 66.9%, 55.0%)

**곡선이 극단적으로 볼록하다.** 상대 카드를 75% 알려줘도 이득 0이고, 100%에서만 튄다.

이유(티츄의 본질): 결정적 실수는 *"상대에게 정확히 무엇이 있나"*에 걸린다 — 폭탄이 있나, 이 랭크를
받아칠 수 있나. **한 장이라도 모르면** 그게 결정적 카드일 수 있어 안전하게 못 둔다.
즉 clairvoyance 이득은 **이진 조건**(전부 알거나 말거나)이지 연속적이지 않다.

→ **어떤 학습된 belief 모델도 부분 정확도이므로 원리적으로 이 헤드룸에 도달 못 한다.**

---

## 3. 닫힌 경로 (재시도 금지 — 전부 실측)

모든 수치는 vs 동결 2단, 짝지은 라운드 점수차 기준.

| 축 | 시도 | 결과 | 왜 닫혔나 |
|---|---|---|---|
| 정보 | 투시 PUCT | 950ms 47.5% / 300ms ~61%지만 **부분정보로 도달불가** | §2 볼록 곡선 |
| 정보 | ③ 상대 패스 추론 | 950ms 40%, **300ms 43.8%** | 크루드 추론은 균일보다 나쁨(틀린 belief) |
| 탐색량 | ~~4배 예산(3800ms)~~ | 50.0% | **⚠ 2026-07-28 재해석: 실제로는 2.1배였다 — 아래 §3-1** |
| 탐색배분 | Sequential Halving | 38.8% | 프라이어를 버려서 |
| 탐색배분 | CRN(챔피언+ decidePlus) | 첫표본 58.8% → **엄격재측 52.4%** | 분산은 줄이나 편향은 못 고침 |
| 모방 | 자기증류 v7/v8 | 55% / 47.5% | 교사를 못 넘음 |
| 정책 | **RL 자가대전 12세대** | **raw +3.85(성공!) 그러나 PUCT 전이 47.3%** | 탐색이 이미 그 수를 둠 |
| 정책 | RL + c=0.5(프라이어 신뢰↑) | 37.5% | 프라이어 과신은 항상 나쁨 |
| 리프 | v_normal 오라클 | 비김 | 플레이아웃과 같은 값 근사 |
| 리프 | v_net 오라클(R²60.5%) | 37.5% | 노이즈가 정확한 플레이아웃을 대체 |
| 리프 | 혼합 50:50 | 48.4% | 희석해서 원위치 |
| 리프 | 신경망 그리디 플레이아웃 | 31.2% | 201배 느림(950ms에 6시뮬) |
| 직접선택 | 1-ply 오라클 argmax | v_normal 12.5%, **v_net 3.8%** | 노이즈 값의 argmax 누적 |
| 보조 | 롤아웃 티츄선언 | 40.0% | 간접비용 무시(§5) |
| 보조 | 선언 임계값 스윕 | 현행이 최적 | 양방향 다 나빠짐 |
| 보조 | 교환 세부(12변형) | 전부 `keep` 이하 | 단순 규칙이 최적 |
| 보조 | 용 증정 | 판돈 자체 없음 | — |
| 보조 | holdValue/declAdjust | 50.0% 중립 | 정상 체제에서도 중립 확인 |
| 구조 | **종반 완전탐색(≤4, 알파베타 K12 다수결)** | 끝단게이트 +5.62±0.48 통과 → **실물전 +0.76±3.69 (50.5%)** | PUCT는 작은 종반에서 이미 거의 정확 — 이기던 상대는 보통봇이었음 |

### 3-1. ⚠ "탐색 포화" 재해석 — repCap 2000이라는 계측 결함 (2026-07-28)

`decidePuct`의 반복 상한 `repCap`은 오라클이 아닐 때 **2000**이다(hybrid-bot.js). 실측:

| 예산 | 실제 시뮬 | 실제 소요 |
|---|---|---|
| 950ms | 941 | 962ms |
| 1900ms | 1806 | 1911ms |
| **3800ms** | **2000 (캡)** | **1901ms — 절반을 안 쓰고 종료** |
| 7600ms | 2000 (캡) | 1846ms |

**따라서 "4배 예산 = 50%"는 실제로 941→2000시뮬(2.1배)을 잰 것이다.** 2000시뮬 너머는
한 번도 측정된 적이 없다. 포화 결론 자체가 틀렸다는 뜻은 아니다 — 2.1배에서 변화가 없던 것은
사실이다 — 그러나 **"예산은 제약이 아니다"라는 일반화는 근거를 넘어선 주장이었다.**

더 중요한 건 배포봇 자신이다. 950ms 606결정 실측:
- 결정의 **49.8%가 캡에 걸려 조기 종료**
- 캡 도달 건의 평균 **미사용 예산 452ms**
- **전체 사고 시간의 23.7%를 버리고 있다**

캡 도입 당시 주석("플레이아웃 ≈650µs라 950ms에 1,500회, 2000이 안 걸린다")은 **평균** 비용
기준이었다. 종반처럼 플레이아웃이 싼 국면에서는 캡이 예산보다 먼저 걸린다 — 분포를 안 본 것이다.

판정은 docs/4DAN-PROTOCOL.md 트랙 C에 사전등록.
**방법론 교훈 추가: 상한·캡·타임아웃은 평균이 아니라 분포로 검증한다.**

---

## 4. 남은 미개척 (낮은 확률, 재개 시 이 순서)

### 4-1. ~~상대-연속 편향 제거~~ — **2026-07-25 킬게이트 실패로 닫힘**

**결과: 30.0% 승률 / 짝지음 −25.02±8.44 (95%CI 상한 −8.48, 기준 +2 미달)**
`oppK=1`(상대 첫 1수만 v6 그리디)로 실행. 원인: 그 정확도를 사려고 세계 표본을 86% 지불
(373 → 51 시뮬). `measure-gap.js`가 예고한 대로(상대 정책 강도 → 리프 값 차이 평균 −0.02)
바뀌는 건 거의 없는데 표본만 날렸다. 구현은 `hybrid-bot.js` `opts.oppK`로 보존(프로덕션 no-op).

<details><summary>원래 계획(참고)</summary>
플레이아웃이 **상대 좌석도 보통봇**으로 둔다. clairvoyance=0은 "내 은닉정보" 축을 닫지만
"상대가 실제보다 약하게 둔다"는 **상대-정책 축**은 직교라 직접 닫히지 않았다.

- 방법: 리프 롤아웃에서 상대 좌석 첫 K수만 v6 net-greedy로 (게이트: 라운드당 3~5 고레버리지 결정만)
- 반대 증거: `measure-gap.js`가 net-greedy 연속 ≈ normal 연속(평균차 −0.02)이라 이미 측정
- 비용: 낮음(v6 재사용, 학습 0, 0.5~1일)
- **사전등록 킬게이트: 첫 80게임 짝지음 95%CI가 +2점/라운드 미달이면 즉시 중단**
- 정직한 확률: 65% 격파 8~12%
</details>

### 4-2. 훨씬 정확한 가치망 (R² 75%+)
현재 v_net R² 60.5%에서 **정체**(8에폭째 거의 안 오름, 177만 샘플).
- 필요: 5~10M 샘플(3~6시간 생성) + 큰 모델, 그래도 불확실
- 반대 증거: R²60%가 37.5%로 졌는데 75%가 50%를 넘긴다는 보장 없음
- 확률: 1~2%

### 4-3. 아키텍처 근본 재설계 (별도 프로젝트)
- 2-ply 트리/ISMCTS: **반대 증거 강함** — `bots.js botPlay`가 자기 손패만 읽어 제거할 strategy fusion이 구조적으로 부재
- 더 큰 관측/모델: clairvoyance 볼록 곡선이 관측 확장을 원리적으로 막음
- 더 강한 하드웨어: 예산을 늘려도 950ms에서 이미 포화(4배 실험)

---

## 5. 방법론 교훈 (다음에 반드시 지킬 것)

1. **배포 예산에서 재라.** 950ms 결론이 300ms에선 틀렸다(정보 포화 ↔ 헤드룸 실재).
2. **CPU 굶주림을 계측하라.** 워크플로 에이전트 13~26개가 동시에 돌면 플레이아웃이 7.7배 느려져
   "개선 없음"과 "탐색이 안 일어남"을 구분 못 한다. `eval-hybrid`가 이제 `perDecision`을 찍는다
   (300ms 정상 = 650~740). `usPerPlayout`은 JIT 워밍업 때문에 부정확할 수 있으니 `perDecision`을 보라.
3. **한 표본에 흥분하지 마라.** 챔피언+ 58.8% → 재측정 52.4%. RL gen2 −1.8 → 재측정 +1.2.
   ⑦교환도 68.8% → 56.2%로 시드마다 튀었다. **항상 다른 시드로 재확인.**
4. **정지 규칙은 신뢰구간 기반으로.** "2세대 연속 음수"가 노이즈로 오발동해 RL을 잘못 죽였다.
   고친 규칙: `평균 + 2·SE < 0`이 2회 연속.
5. **순환논증 금지.** 정답을 결정과 같은 절차로 정의하면 안 된다(롤아웃 티츄가 그래서 실패).
6. **대리 지표 ≠ 실물.** "강한 상대"를 신경망 그리디로 대신했다가 실전(탐색 상대)에서 −10%p.
7. **짝지은 라운드 점수차를 써라.** 승단전 80판(15분, SE 5.5%p) 대신 3000딜(86초, SE 1.5%p).
   화폐환산: **1점/라운드 ≈ 0.72%p 승률**.
8. **"확률>50%면 베팅"은 티츄에서 틀렸다.** 티츄 선언은 파트너가 자기 완주를 늦추고 상대가 집중
   저지하는 **간접 비용** 때문에 진짜 손익분기가 50%보다 훨씬 위다.

---

## 6. 자산 (재개 시 바로 쓸 것)

### 측정 도구
| 파일 | 용도 |
|---|---|
| `ml/eval-hybrid.js` | 승단전. `TICHU_CAND`/`TICHU_OPP_CAND`(사다리), `TICHU_PERFECT`(투시), `TICHU_PIN`(부분정보), `TICHU_ORACLE`(리프), `PAIRS`(짝지은 점수차), `SIMS`(굶주림 계측) |
| `ml/eval-raw.js` | raw 그리디 짝지음 — 정책 비교용(200배 빠름). v6vsv6=0.000 검증됨 |
| `ml/eval-exchange.js` | 교환 전략 짝지음(3000딜 86초) + 점수출처 분해(카드/티츄/원투) |
| `ml/eval-declare.js` | 선언 임계값 2D 스윕 |
| `ml/gate-headroom.js` | 정책 argmax → 플레이아웃 최선수 swing(승자의저주 제거) |
| `ml/decision-swing.js` | 비플레이 결정(용·소원·선언) 판돈 측정 |
| `ml/measure-gap.js` | 롤아웃 정책 강도가 리프 값에 주는 영향 |

### 학습 파이프라인
| 파일 | 용도 |
|---|---|
| `ml/gen-rl.js` | 자가대전 생성(학습팀 vs 동결). 오라클 상태baseline `ov` 기록 |
| `ml/train_rl.py` | PPO + 오라클 baseline + **적응형 KL β**(목표KL로 자동조정) + 엔트로피 바닥 |
| `ml/rl3.sh` | RL 루프(생성→학습→내보내기→신뢰구간 트립와이어) |
| `ml/gen-oracle.js` | 완전정보 가치망 데이터(`normal`/`net:`/`puct:` 라벨 정책) |
| `ml/train_oracle.py`, `shared/oracle-value.js` | 오라클 학습·JS추론(290차원 희소 인코더) |
| `ml/check_oracle.py`, `ml/check-oracle.js` | 파이썬↔JS 수치 대조(전치 버그 방지) |

### 산출물
- `ml/data/rl3/gen1~12.{pt,json}` — RL 세대별 정책(gen12가 raw 최강, +3.85)
- `ml/data/orc/v_normal.{pt,json}` (R² 61.6%), `v_net.{pt,json}` (R² 60.5%)
- `ml/data/orc/net*.bin` (2GB, net 정책 오라클 데이터 177만 샘플) — 재생성 1시간

### 봇 옵션 (전부 프로덕션 no-op, 게이트 뒤)
`shared/hybrid-bot.js`: `decideSH`(Sequential Halving+CRN), `decideOracle1ply`, `declareTichu`(롤아웃 선언),
`oracleEval`, `neuralPlayout`, `opts.perfect`(투시), `opts.playout='neural'`, `globalThis.__TICHU_STAT`(시뮬 계측)
`shared/bots.js`: `botExchange(g, s, {keepSpecials})`(=2단 교환), `globalThis.__TICHU_PIN`(부분정보 진단)

---

## 7. 만약 다시 시작한다면 (권장 순서)

1. **먼저 §5 교훈을 읽어라** — 특히 배포 예산·굶주림·시드 재확인.
2. **4-1(상대-연속 편향)을 사전등록 킬게이트와 함께** 하루 실험. 실패가 모달 결과.
3. 실패하면 **2단이 상한**임을 받아들이고, 대신 **체감 개선**(사람에게 더 재미있는 봇)으로 방향 전환 고려:
   - 봇 성격/속도 다양화, 채팅 반응, 난이도 조절(일부러 약하게)
   - 이건 "강함"이 아니라 "즐거움" 축이라 천장이 다르다.
4. 근본적으로 더 강한 봇을 원하면 **다른 하드웨어/프레임워크**(GPU 추론 서버, 큰 모델, 초 단위 예산)가
   전제다 — 지금 구조(순수 JS, Render 0.1CPU, 300ms)의 문제가 아니라 그 제약 자체를 바꿔야 한다.
